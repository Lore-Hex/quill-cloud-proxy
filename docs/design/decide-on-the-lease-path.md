# Decide on the spend-lease path

Date: 2026-09-25. Status: design proposal; no production changes authorized by this document.

## Recommendation and authority

Implement **Step 1a: speculative decide execution behind the existing Stage C reserve gate**, with a separately enabled decide cohort. Implement **Step 1b: reply before settlement** as a separate, default-off, bounded-loss pilot requiring Joseph's approval. Step 1b cannot preserve the present billing-completion/error contract in every failure. Neither step should silently enroll decide in Stage D. **Step 2: reply on local admission alone** is an explicit alternative for Joseph, not the default or an interpretation of Stage C.

The supplied v67 design history is authoritative: Stage D narrowed to Credits, standard endpoint pricing, streaming Chat/Responses; other requests retain their existing behavior. The router-outage availability addendum was withdrawn. This document proposes specific exceptions for decide and does not revive that addendum by implication. The v67 record was not found in the two local `origin/main` trees; its history and the measurements below are supplied task evidence, not claims of independently inspected production state.

All code citations use the following immutable local snapshots. `E:path:line` means `Lore-Hex/quill-cloud-proxy` **origin/main at `b838ed4eccc6ec9ce0143e53f119c127112daa8a`**; `R:path:line` means `Lore-Hex/quill-router` **origin/main at `be3661239a2d5e7b6e787d80dd5fc5c7d78675eb`**. Lines refer to those blobs, not the eventual implementation branch. No network access or production inspection was used.

The public scope is every path recognized by `isDecidePath`: `/api/alpha/decide`, `/api/decide`, `/v1/decide`, `/v1/evaluate`, `/api/alpha/decisions`, `/api/decisions`. All use internal route type `decide`; setting `stream=true` remains invalid. An internal native provider stream does not make the public request streaming. (`E:enclave-go/cmd/enclave/decide.go:252`, `E:enclave-go/cmd/enclave/decide.go:292`.)

## Baseline and latency budget

Supplied measurement, 2026-09-24, keep-alive from us-central1: hosted `typesafe-ai/jev` takes 553 ms end to end through the us-central1 enclave, with approximately 100 ms in the vendor and approximately 453 ms attributed to the control plane. There are no supplied separate authorize/settle distributions; do not divide 453 ms equally and call that a measurement. A1 separately targets a 200–300 ms enclave/control-plane round trip from each region.

Today hosted decide calls `AuthorizeEmbeddingsWithRoute`, invokes the vendor, verifies, then calls `Settle` before writing JSON. Native decide constructs a chat request, enters `runFusionCallValidated`, authorizes through `authorizeFusionCall`, collects and validates the generation, then waits for `settleAndBroadcast`. (`E:enclave-go/cmd/enclave/decide.go:390`, `E:enclave-go/cmd/enclave/decide.go:488`, `E:enclave-go/cmd/enclave/decide.go:529`, `E:enclave-go/cmd/enclave/decide.go:599`, `E:enclave-go/cmd/enclave/fusion.go:1973`, `E:enclave-go/cmd/enclave/fusion.go:2084`, `E:enclave-go/cmd/enclave/fusion.go:2123`, `E:enclave-go/cmd/enclave/fusion.go:2722`.)

Let R be a successful reserve latency, M the entire provider attempt including any fallback, S the settlement wait, and L local validation, buffering, encoding and scheduling. Exclude client/enclave transit from these equations. For a warm eligible attempt:

| Mode | Time until answer can be written | Meaning |
| --- | --- | --- |
| Today | R + M + S + L | Two serial control-plane waits |
| Step 1a only | max(R, M) + S + L | Speculation overlaps reserve, settlement still blocks |
| Step 1a + 1b | max(R, M) + L | Reserve still gates the first byte |
| Step 2, optional | M + L | No per-request router acknowledgement before reply |

For M = 100 ms and R = 200–300 ms after A1, Step 1a + 1b is approximately 200–300 ms plus L and client transit, **not 101 ms**. Its added critical-path latency over the model is approximately 100–200 ms plus L. If M exceeds R, Step 1 can hide R, but that is workload dependent. If each of today's two waits were 200–300 ms, the corresponding serial budget would be 500–700 ms; that is an illustration, not a forecast replacing the 553 ms observation. Two native attempts have two admission/provider timelines and two billing records; the single-attempt formula is not a retry-path SLO.

Define the desired ~1 ms as *warm-path TrustedRouter processing overhead relative to identical provider work*, including local admission and verification, not total decide time or internet transit. Measure M, R, their overlap, post-model gate wait, settlement start/durability/finalization, local CPU and response-write time independently at p50/p95/p99. Report warm lease hits separately from cold start, rotation, policy miss and ordinary authorization. Serialization of a large state and cryptographic signing might themselves exceed 1 ms; benchmark before promising it.

## What the existing lease protocol actually guarantees

Stage C is not offline billing authority. `PrepareSpendLeaseAdmission` performs local eligibility/credential checks, uses the signed catalog and conservatively decrements local remaining capacity; nil means ordinary synchronous authorization **before** provider work. `ReserveSpendLeaseAdmission` submits the signed receipt, binds an authorization at the router, and checks the acceptance marker, receipt hash, remaining amount, identity and ordered dispatch candidates. Only that marked response opens the gate. The chat implementation cancels speculation on errors and on unmarked responses. (`E:enclave-go/internal/trustedrouter/stage_c.go:34`, `E:enclave-go/internal/trustedrouter/stage_c.go:151`, `E:enclave-go/internal/trustedrouter/stage_c.go:196`, `E:enclave-go/internal/trustedrouter/stage_c.go:223`, `E:enclave-go/cmd/enclave/main.go:1230`.)

Local admission requires authoritative/local-admission claims, the registered boot key, matching policy hash, an active grant and an unexpired admission window. CAS decrements are never restored, even on cancellation/refusal. `Release` releases only the in-flight idempotency scope. Reserve observations only reduce remaining capacity. A signed token alone, or a `spend_lease_echo`, is not permission to return an answer. (`E:enclave-go/internal/spendlease/state.go:149`, `E:enclave-go/internal/spendlease/state.go:173`, `E:enclave-go/internal/spendlease/state.go:205`, `E:enclave-go/internal/spendlease/state.go:253`, `E:enclave-go/internal/spendlease/types.go:136`.)

Current tokens have a maximum 60-second TTL, a 28-second admission margin/reserve budget and 10-second skew; verification constructs monotonic deadlines and binds identity. The router verifies the boot, receipt, lease generation, policy, catalog estimate and acceptance flag, then reserves through the typed binding machinery. Preserve byte-stable retries and the original billing authority. (`E:enclave-go/internal/spendlease/types.go:9`, `E:enclave-go/internal/spendlease/verifier.go:95`, `E:enclave-go/internal/spendlease/verifier.go:112`, `E:enclave-go/internal/spendlease/verifier.go:149`, `E:enclave-go/internal/trustedrouter/spend_lease.go:183`, `R:src/trusted_router/routes/internal/gateway.py:2741`, `R:src/trusted_router/routes/internal/gateway.py:1586`.)

Stage D is narrower still. Router eligibility requires streaming Chat/Responses, Credits, standard pricing, accepted boot and supported settlement backend; priority/auto are excluded. Enclave eligibility repeats the public stream/route gate. It sends a pre-header heartbeat, meters slices, terminates at cap or heartbeat loss, and makes a bounded settle attempt before terminal output. Heartbeats atomically validate endpoint, monotonic usage/sequence and cap, store delivered usage and renew the hold. Decide cannot obtain these guarantees by passing through a chat helper. (`R:src/trusted_router/routes/internal/gateway.py:3103`, `R:src/trusted_router/storage_gcp_stage_d.py:47`, `R:src/trusted_router/storage_gcp_stage_d.py:91`, `R:src/trusted_router/storage_gcp_stage_d.py:121`, `E:enclave-go/cmd/enclave/stage_d.go:50`, `E:enclave-go/cmd/enclave/stage_d.go:157`, `E:enclave-go/cmd/enclave/stage_d.go:198`, `E:enclave-go/cmd/enclave/main.go:1890`.)

## Step 1a: decide-specific Stage C admission

### Cohort, catalog and request contract

Start with Joseph's workspace `45819281-0ce9-4811-a0cd-c660ab3a116d`, a paid/trust-eligible key without lifetime/daily/weekly/monthly limits, Credits on **every** fallback candidate, standard endpoint token/request pricing, default service tier, no batch, BYOK, custom/user model, partner mode, surcharge, app markup, receipt fee or regional/deferred settlement. Confidential requests and unsupported provider policies keep synchronous authorization. Preserve trust pause, reconciliation freshness, boot attestation and escrow gates. Paid status alone does not imply lease eligibility. Existing exclusions are in `R:src/trusted_router/spend_leases.py:236`, binding's deferred exclusion in `R:src/trusted_router/spend_leases.py:110`, trust gating in `R:src/trusted_router/routes/internal/gateway.py:1636`, and confidential exclusion in `E:enclave-go/internal/trustedrouter/stage_c.go:44`.

This needs coordinated changes, not just two calls inserted in `decide.go`:

1. Add a flag-gated decide cohort to both issuance eligibility and receipt-bearing acceptance. Both currently exclude decide (`R:src/trusted_router/spend_leases.py:41`, `R:src/trusted_router/routes/internal/gateway.py:1515`). Keep Stage D eligibility unchanged. Give decide grants an explicit versioned cohort, proposed `credits-decide-v1`; deploy recognition in both verifiers before minting it. Do not broaden the meaning of `credits-chat-v1` invisibly.
2. Freeze candidates from the existing router selector, with `route_type="decide"`, region, service tier, endpoint ID, concrete model, upstream model, provider, Credits mode, privacy dispatch bits, ordered fallback chain, tier/rate/request-fee fields and catalog hash. No live catalog/provider lookup may widen that set during speculation. Today's catalog already records these applicability/price/dispatch fields (`R:src/trusted_router/spend_leases.py:342`); estimator and local authorization match route/model/region/tier and select the maximum applicable endpoint estimate (`E:enclave-go/internal/spendlease/estimator.go:10`, `E:enclave-go/internal/trustedrouter/stage_c.go:99`). A chat grant is a miss for decide, even on the same underlying model.
3. Extend routing normalization narrowly and add shared Python/Go fixtures. Today Go rejects non-chat routes, all `trustedrouter/` models, nonempty response-model metadata, nil provider and non-explicit Credits; native's generated provider pin does not set Credits. Hosted authorization has no provider object at all. Never manufacture `provider.usage=Credits` merely to pass the guard if it changes the ordinary route choice. The decide normalization must reproduce router defaults and prove the resolved whole candidate set is Credits; otherwise miss. (`E:enclave-go/internal/trustedrouter/routing_policy.go:20`, `E:enclave-go/internal/decide/chat.go:314`, `R:src/trusted_router/routing.py:841`.)
4. Add a shared admission descriptor separating the original public request/model from the immutable routing/estimate inputs. Native uses the existing chat estimate builder; hosted uses a decide/embedding builder. Reserve must serialize the **same** estimate and policy that were signed, before dispatch rewrites `req.Model`. The existing Stage C wire body drops ordinary attribution fields; therefore initially exclude requests carrying tags/user/session/trace/metadata/app attribution unless a reviewed decide wire extension preserves their authorization and settlement semantics. Do not silently lose tags or app-based charges for speed. (`E:enclave-go/internal/trustedrouter/spend_lease.go:235`, `E:enclave-go/internal/trustedrouter/client.go:878`, `E:enclave-go/cmd/enclave/main.go:1278`.)

Grants are demand-filled: first/cold/mismatching request takes ordinary authorization and can receive a compatible grant for later calls. Current state keeps one active grant per key and declines replacing a live grant; switching model, route or policy can therefore produce sustained misses until expiry. Do not claim a universal paid-plan hit rate or add a new multi-grant cache in this change. (`E:enclave-go/internal/spendlease/state.go:290`.)

### Private decision aliases are a separate compatibility gate

The named aliases are `trustedrouter/{trev,mev,zev,lev,gev,dev,oev,gemmev}-1.0`. They are **private-proxy routes**, not interchangeable public names for arbitrary chat endpoints. Their backing models and permitted chains are in `R:src/trusted_router/catalog_data.py:2326`; inclusion in `PRIVATE_PROXY_MODEL_TARGETS` is at `R:src/trusted_router/catalog_data.py:2373`. For example Trev is gpt-oss-120b on Cerebras/SambaNova/Fireworks/Together in that order. Router authorization enforces the chain even if caller preferences request another order (`R:src/trusted_router/routes/internal/gateway.py:1143`, `R:src/trusted_router/routes/internal/gateway.py:1211`).

A lease for an alias must bind **both** the exact public alias and its concrete dispatch model. Router normalization expands private aliases to their backing model (`R:src/trusted_router/routing.py:992`, `R:src/trusted_router/routing_candidates.py:388`), whereas existing local matching compares `req.Model` directly to catalog candidates. Simply removing the Go prefix exclusion would not fix this mismatch. Proposed signed decide routing envelope: `requested_model`, `resolved_model`, `response_model`, `hide_public_metadata`, and a version for the alias/chain mapping, in addition to the existing policy hash and catalog. Bind the requested alias in the policy hash so two aliases sharing weights cannot share authority accidentally. For ordinary/hosted models requested and resolved IDs coincide and hiding is false.

Build `plan.Local` with the concrete model for dispatch and the signed alias metadata for response construction. Extend acceptance matching to all those fields. Use the router's pinned chain and permitted caller filters, not a fresh expansion in the enclave. Preserve public selected model = alias, provider = `trustedrouter`, endpoint omitted. The ordinary router returns that metadata today, and decide applies hiding after any settlement override (`R:src/trusted_router/routes/internal/gateway.py:2993`, `R:src/trusted_router/routes/internal/gateway.py:3024`, `E:enclave-go/cmd/enclave/response_metadata.go:364`). Initially leave aliases synchronous until these fixtures pass; do not quietly exclude them from the final scope or leak their backing route while accelerating them.

### Native attempt lifecycle

Build and validate `decide.NativeChatRequest` first, including prompt/schema, reasoning options, provider constraints and explicit token budget; normalize the same reasoning cap as `authorizeFusionCall` before preparing admission. The builder caps output at 32,768 tokens, but its prompt estimate is not a hard billing upper bound (`E:enclave-go/internal/decide/chat.go:269`, `E:enclave-go/internal/decide/chat.go:274`, `E:enclave-go/cmd/enclave/fusion.go:2714`, `E:enclave-go/internal/trustedrouter/client.go:1451`).

For **each** of at most two attempts:

1. Assign its idempotency key before preparation. Keep attempt 1's key and attempt 2's `:decide-retry-2` suffix; generate a nonempty key when absent. Freeze it through reserve transport retries, settlement and repair. Do not use attempt 1's allocation for attempt 2. Current retry/billing semantics are explicit at `E:enclave-go/cmd/enclave/decide.go:590`.
2. Prepare using route `decide`. On a miss, use today's synchronous `authorizeFusionCall`. On a plan, start the ordinary provider fallback machinery with `plan.Local` concurrently with reserve. Refactor the fusion helper into authorize / invoke-and-collect / prepare-finalization / finalize seams; select this mode only for decide, not all fusion callers.
3. Collect into a bounded, private buffer while reserve runs. Do not merely leave the existing `io.Pipe` unread: it can block the first provider write and prevent whole-model overlap. Retain the per-attempt generation recorder, selected-route tracker, upstream counts, limits, deadline and cancellation. Never attach the speculative writer to the client socket. Match the selected endpoint against the accepted frozen candidates before using the result.
4. After accepted reserve, apply `recorder.complete` **before deciding settle versus refund**. Incomplete/error/empty generations are refunded and may consume the remaining attempt. A complete generation is billed even when `ExtractNative`/`Verify` rejects its answer; those checks happen before any public answer but do not turn native malformed answers into free calls. Accumulate usage of completed attempts and retain each attempt's finalization job. Two invalid completed attempts yield a spent error. Control-plane verdicts do not consume the second attempt as a workaround. (`E:enclave-go/cmd/enclave/decide.go:124`, `E:enclave-go/cmd/enclave/decide.go:604`, `E:enclave-go/cmd/enclave/decide.go:632`, `E:enclave-go/cmd/enclave/fusion.go:2072`.)

Keep native provider fallback before its first provider byte; after a stream has committed, a broken generation follows the existing refund/new-attempt rules. An ordered candidate change inside an attempt uses the same authorization, not a new lease allocation. The outer validation retry is a new billed attempt. These are different counters and must stay different (`E:enclave-go/cmd/enclave/decide.go:74`, `E:enclave-go/cmd/enclave/decide.go:600`).

### Hosted attempt lifecycle

Add `PrepareHostedDecideSpendLeaseAdmission` / `ReserveHostedDecideSpendLeaseAdmission` (names proposed), sharing signature, CAS, marker and transport code with Stage C. Do not pass a dummy empty chat request. Use the current embedding-style estimate of state plus serialized questions, minimum one input token, and explicit output estimate **1**, not the chat default 512; the existing API sends `max_output_tokens=1`, while the Stage C canonical contract uses `max_tokens`. Normalize those to the same router `output_estimate`, and pin fixture parity. Route remains `decide`, not `embeddings`. (`E:enclave-go/cmd/enclave/decide.go:385`, `E:enclave-go/internal/trustedrouter/client.go:848`, `E:enclave-go/internal/trustedrouter/spend_lease.go:235`.)

Execute `InvokeDecide` concurrently with reserve into a bounded result slot. Retain current fallback order and upstream error classification. Jev has a direct TypeSafe candidate and Vercel relay, each with its own upstream ID; the catalog currently prices both at $0.042 per million input tokens and does not meter output (`R:src/trusted_router/catalog_data.py:3149`). Verify the result before any success or settlement; invalid hosted answers are **refunded**, return 502 with `x-should-retry:false`, and do not get the native validation retry. Preserve missing-input estimate and the byte-length-plus-4096 reported-input ceiling; preserve output tokens in the response but zero output tokens for billing. Settlement names the actually served endpoint. (`E:enclave-go/cmd/enclave/decide.go:430`, `E:enclave-go/cmd/enclave/decide.go:488`, `E:enclave-go/cmd/enclave/decide.go:502`, `E:enclave-go/cmd/enclave/decide.go:522`.)

### Reserve gate outcomes, including races

| Outcome | Required behavior |
| --- | --- |
| Local miss/signing failure/expired or exhausted grant | No speculation; ordinary authorization first. Local decrement, if already made, remains spent. |
| Marked, matching accepted reserve | Adopt the returned authorization ID and pinned control-plane endpoint. Reuse buffered result/provider stream; never dispatch it a second time. |
| Explicit refusal, malformed marker or timeout/unreachable router | Cancel provider, close/drain buffers and discard answer. Return control-plane failure; no fallback to an unreserved public success. Resolve an ambiguous reserve using identical retry identity; never settle a local authorization with an empty ID. |
| Valid but unmarked authorization from older router | Cancel/discard speculation, adopt and validate the ordinary returned authorization, then redispatch from its candidates. Do not reauthorize with a new key or bill the discarded speculative call. |
| Provider fails/completes before reserve | Store result/error until reserve classification. If an authorization is obtained, finalize it exactly once according to the above rules; if none is obtained, account speculative vendor loss operationally. |
| Client disconnects during either operation | Cancel unnecessary provider work; reconcile any returned authorization with detached finalization. A completed accepted generation remains billable under today's native/hosted rules. |

These follow the chat precedent at `E:enclave-go/cmd/enclave/main.go:1234` and marker checks at `E:enclave-go/internal/trustedrouter/stage_c.go:174`. Cancellation is not proof the vendor stopped or waived charges. Keep explicit 401/402/429 and Retry-After semantics; if a complete discarded generation makes a final error unsafe to regenerate, attach `x-should-retry:false` without changing its status/source. That header means provider work was spent, **not** that customer settlement committed (`E:enclave-go/cmd/enclave/decide.go:188`, `E:enclave-go/cmd/enclave/http_io.go:696`). Do not turn a reserve refusal into a second native attempt. Any ambiguous accepted hold that never obtains a usable ID may ultimately reap unbilled.

### A billing consequence even before async settlement

Stage C's estimate is a reservation, not an exact price. Spend-lease settlement deliberately caps the customer charge at `min(allocated_micro, estimated_microdollars)`; actual cost above it is absorbed, not charged later (`R:src/trusted_router/services/spend_lease_settlement.py:94`). Therefore adding decide to spend leases can change the charge even with synchronous settle. Hosted's ordinary estimate can be below vendor-reported input; native estimates omit some provider formatting/schema overhead and cannot prove token counts. Do not advertise exact billing equivalence for this extension.

For Step 1 retain existing metering and decision-50 clamp; expose estimate/actual/clamp deltas and seek approval for this bounded customer discount before enabling the cohort. Do not inflate usage to the reservation or retrofit an overage bill. A later conservative-reservation variant could allocate a separately represented worst-case cost while preserving actual usage and pricing tiers, but changing `estimated_input_tokens` to a byte ceiling blindly would affect applicability. That requires shared estimator/schema tests, not an undocumented optimization. The hard bound required for Step 2 is stronger than Step 1's estimator.

## Step 1b: reply before settlement

### Decision: opt-in exception, not transparent reordering

For eligible, accepted, non-batch decide, prepare the full response from validated answer, measured usage and actual selected route, then transfer every required finalization to a supervised worker **before** writing the response. The worker uses a detached service/shutdown context and starts settlement immediately; it does not wait for socket close. Step 1a may ship with blocking settlement first. All ineligible paths retain today's ordering.

The normal success body can be equivalent: decide's only ordinary settlement-derived fields are selected model/provider, already known from the served route. Preserve exact public alias hiding and counters; do not use the primary authorization endpoint when a fallback served. Add byte/field parity tests across both implementations. Batch is different: `annotateBatchSettlementOnlyUsage` adds monetary fields and usage type from settlement, so **exclude all batch execution contexts**, not only native-batch-eligible authorization. (`E:enclave-go/cmd/enclave/decide.go:673`, `E:enclave-go/cmd/enclave/response_metadata.go:364`, `E:enclave-go/cmd/enclave/response_metadata.go:145`, `E:enclave-go/cmd/enclave/response_metadata.go:191`.)

The failure contract cannot be identical. Today a hosted settle failure returns spent 502; native enqueues repair and returns `settlementAttemptedError`, also stopping the native retry. After a 200 is written that error cannot be substituted. Step 1b explicitly changes “settlement failed after a valid answer” into “answer succeeded, billing pending or possibly lost.” If Joseph requires zero additional unbilled answers or the same error contract, keep S on the critical path (or wait for durable router intent, which still costs a round trip). (`E:enclave-go/cmd/enclave/decide.go:529`, `E:enclave-go/cmd/enclave/fusion.go:2123`.)

For a completed invalid native attempt, hand off its settlement before starting attempt 2. This introduces one further difference: attempt 2 can run while attempt 1's settlement is failing. Restrict it to a fresh valid lease allocation; never evade a refused authorization. With async mode off or handoff failure, use today's blocking settle and stop on failure. Joseph's Step 1b approval must include this difference, rather than claiming retries are unchanged under all failures.

### Queue and durable outbox are different guarantees

Current `settlementRetries` has capacity 1024, defaults to six attempts, is process memory, may drop its oldest job to enqueue a new one, and eventually gives up. Shutdown draining helps only graceful exits. It compacts jobs by stripping attribution; its worker calls `Settle` directly and does not replay `settleAndBroadcast`'s content broadcast. Native enqueues on initial settle failure; hosted's direct settle currently does not. The transport's own three retries/28-second budget are also not durable storage. (`E:enclave-go/cmd/enclave/settlement_retry.go:15`, `E:enclave-go/cmd/enclave/settlement_retry.go:85`, `E:enclave-go/cmd/enclave/settlement_retry.go:112`, `E:enclave-go/cmd/enclave/settlement_retry.go:195`, `E:enclave-go/cmd/enclave/settlement_retry.go:269`, `E:enclave-go/internal/trustedrouter/settlement_retry.go:12`.)

Do not call existing `Enqueue` and equate its true return with a retained billing obligation. Add a decide-specific, bounded **non-evicting** handoff class with admission tickets acquired before speculation. Reserve a slot per attempt, transfer immutable usage, authorization, selected endpoint, fixed request ID, request-log/client context and required attribution before reply, and release the slot only on terminal classification or explicit recorded loss. A full/disabled handoff class means blocking settle for that request. Use a bounded worker pool and per-attempt deadlines so one hung settle cannot block the entire class; the ticket count covers queued and in-flight jobs. Never let ordinary retry pressure evict an already-replied obligation; exhausted retries occupy the loss budget and trip the async circuit breaker. Keep prompts, answers and provider secrets out of billing jobs; attribution retention needs the existing privacy policy and bounded sizes. Initially exclude broadcast-dependent requests unless equivalent worker behavior is tested. Add the hosted repair path as well.

Router outbox durability begins only when the settle handler commits its insert; it freezes resolved cost and settlement body and protects the hold from reaping. It does not know a result still queued inside an enclave. Its drain has a 60-second initial delay, so even durable intent is not immediate ledger/generation visibility (`R:src/trusted_router/routes/internal/gateway.py:4038`, `R:src/trusted_router/routes/internal/gateway.py:4073`, `R:src/trusted_router/storage_gcp_authorize.py:1104`).

Handle outcomes semantically, not merely HTTP 200:

| Router outcome | Worker/accounting interpretation |
| --- | --- |
| `finalized` | Terminal money operation committed; record outcome/cost/generation. |
| `intent_durable` | Router owns durable retry responsibility; transfer out of enclave queue, monitor outbox to terminal. Do not claim final charge/generation yet. |
| `already_finalized` | Inspect finalization outcome: it may be settled **or refunded**. Never charge again. An already-refunded completed call is a lost settlement. |
| `reaped_snapshot` | Reaper won; late actual usage does not replace its charge. Record `settle_lost`; do not broadcast this answer as that snapshot generation. |
| Error/timeout/missing or unknown disposition | Bounded idempotent retries against original authority; then explicit unresolved/loss accounting and async breaker, never a fresh charge. |

The router emits these dispositions and finalization outcomes (`R:src/trusted_router/routes/internal/gateway.py:4638`, `R:src/trusted_router/routes/internal/gateway.py:4677`, `R:src/trusted_router/routes/internal/gateway.py:4705`). Enclave already treats reaped snapshots and `AlreadySettled && !Settled` as lost, and suppresses broadcasts without a generation or after loss (`E:enclave-go/cmd/enclave/stage_d.go:402`, `E:enclave-go/cmd/enclave/settlement.go:38`). Do not use the disposition lookup alone as proof an outbox row exists: `_current_disposition` returns `intent_durable` for an unsettled record without checking the outbox. A timed-out settle needs retry or positive durable-intent evidence, not that inference.

### Receipts and clients, including AnyEval

Admission receipts authorize the internal optimization; they are not client inference receipts or proof of a final charge. `receipt_emission.go` can sign request/response digest and selected route, with a supplied generation ID, but decide currently calls plain `writeJSONResponse`, not the receipt writer. Do not synthesize a generation ID or signed billing receipt before settle; keep receipt-bearing features out of the pilot until their contract is explicitly covered. (`E:enclave-go/cmd/enclave/receipt_emission.go:41`, `E:enclave-go/cmd/enclave/receipt_emission.go:110`, `E:enclave-go/cmd/enclave/decide.go:686`.)

Preserve the `model`, `answers` (including requested `noul` spelling), usage totals, routing fields and error headers that response consumers can read. `x-should-retry:false` still applies to final invalid-generation/encoding/spent errors, including errors after an earlier billed attempt; a settlement failure after a 200 cannot be communicated through a later header. Retrying the same key is not a guaranteed cached-answer read; current invocation-nonce/scope checks prevent replay from launching another provider invocation (`E:enclave-go/internal/trustedrouter/spend_lease.go:206`).

AnyEval source was not found by case-insensitive search of either complete `origin/main` tree. We cannot certify what it polls or how it reacts to delayed activity. The public generation lookup requires a generation ID and returns 404 if unavailable; ordinary decide has no generation ID in its response. Internal request-log evidence can return pending and can be ambiguous for multiple authorizations, including native attempts. (`R:src/trusted_router/routes/activity.py:71`, `E:enclave-go/cmd/enclave/decide.go:230`, `R:src/trusted_router/routes/internal/gateway.py:701`.)

Release gate: run the actual AnyEval adapter/harness against matched blocking/async fixtures, including 200 followed by delayed settlement, two native attempts, lost acknowledgement and refunded/reaped outcomes. Verify response use and any usage/credit/activity/generation follow-up reads. Pending or missing records must not be interpreted as zero cost or trigger regeneration. Keep an internal per-attempt authorization correlation; do not overload the single request-log lookup. Clients requiring settlement visibility at return stay on the blocking cohort. This is an unresolved compatibility gate, not evidence that AnyEval is compatible.

### Crash cases and money at risk

Today the ordinary reaper releases stranded holds with `success=False`: **unbilled**, even if the provider ran. Stage D snapshot booking requires its flag, a started marker and pricing snapshot; decide has neither Stage D cohort eligibility nor its heartbeat snapshots. Merely enabling `TR_REAP_SNAPSHOT_BOOKING_ENABLED` does not protect decide. The checked-in rollout sets this flag true, while the Settings default is false; neither changes decide's exclusion. (`R:src/trusted_router/storage_gcp_authorize.py:1089`, `R:src/trusted_router/storage_gcp_authorize.py:1301`, `R:src/trusted_router/storage_gcp_authorize.py:1409`, `R:src/trusted_router/config.py:974`, `R:scripts/deploy/rollout.sh:690`.)

| Failure boundary | Consequence |
| --- | --- |
| Death after reserve, before provider completion | Existing stranded hold; unbilled reaping, possible provider expense. |
| Death after answer/handoff, before router outbox commit | New Step 1b exposure: delivered valid answer, lost in-memory usage, no durable settle intent; reaper refunds. |
| Death after outbox commit, before acknowledgement | Durable router repair survives; retry same authorization, no duplicate customer charge. |
| Reaper wins before late settle | Finalized refund today for decide; late settle cannot resurrect a charge. A future snapshot extension would instead preserve snapshot charge and report lost final usage. |

Let P_i be the ordinary priced usage for completed billable attempt i, A_i its lease allocation/estimated cap, and C_i = min(P_i, A_i) the spend-lease charge. One lost finalization loses **C_i microdollars of collectible revenue**, at most A_i. One native request can lose C_1 + C_2 if both completed attempts are still pending; a hosted request has one successful billable attempt. Separately record decision-50 undercharge max(0, P_i − A_i). Provider invoices V_i, including failed/discarded fallbacks, are a separate expense; customer lease caps do not prove V_i ≤ A_i. Do not add revenue loss and invoice expense as if they were the same accounting quantity.

Concrete scale: Jev's checked-in $0.042/M input rate means 1,000 billed tokens cost 42 microdollars = **$0.000042**; 32,000 cost 1,344 microdollars = **$0.001344** before a lower lease clamp. Its actual caller-charge ceiling from code is `round_half_up(0.042 × (state_bytes + question_json_bytes + 4096))` microdollars, subject to the positive-charge floor and A_i. 32k is the catalog context size, **not a proven absolute charged-token bound**: the handler clamps against bytes plus template allowance. Native must use its selected endpoint's input/output/cache rates and actual usage; 32,768 max output tokens alone does not bound its dollars. (`R:src/trusted_router/catalog_data.py:3161`, `E:enclave-go/cmd/enclave/decide.go:511`, `E:enclave-go/internal/spendlease/estimator.go:79`.)

Proposed initial async limits: **A_i ≤ 10,000 microdollars ($0.01) per attempt, at most 100 outstanding attempt tickets per enclave**. Thus at most $0.02 collectible revenue per two-attempt native request and $1 per enclave crash across that handoff class, further bounded by the capacities of the grants backing those obligations, including draining or expired grants still awaiting reconciliation. Count a worker's in-flight job, not only queued jobs. These are proposed rollout limits, not current configuration or a bound on arbitrary native vendor invoices. Set a separate $1 pilot-wide daily lost/unresolved-charge stop threshold, counting missing post-reply evidence conservatively; renewal and repeated crashes make a per-lease cap insufficient for cumulative loss. This is an operational stop threshold, not a hard global loss ceiling: detection delay and other enclaves' outstanding tickets add exposure. A hard $1 global ceiling would require partitioned, durably tracked risk tickets and withholding their renewal until reconciliation. Require an external observer because an enclave can lose its own counters on death. Any tighter invoice bound needs provider-specific caps and evidence before expansion.

Do not “fix” the crash gap by charging every expired decide allocation at its estimate: invalid hosted answers must refund, unused reservations are not consumption, and native incomplete generations refund. Exact eventual charging requires durable completed usage before reply (a synchronous durable handoff/round trip) or a new reviewed recovery/billing policy. Expanding Stage D to non-streaming decide is such a proposal, not a flag flip; an early heartbeat proves started input, not a valid delivered decision.

## Step 2 option: local authority to release an answer

**Recommendation: defer.** Warm Step 2 would return after local signed-lease admission and model verification, while reserve/settle run asynchronously. Even if reserve normally starts concurrently, an answer may beat its acknowledgement or survive its failure. This directly relaxes the converged fail-closed first-byte rule and reinstates the central risk of the withdrawn availability addendum. A “router looked healthy recently” check cannot distinguish slow from unreachable at the reply boundary. Joseph must explicitly authorize a decide-only exception; otherwise Step 1's router gate remains mandatory.

Scope, if approved: only the pilot workspace, paid/trust-eligible Credits keys, standard default-tier pricing, vetted bounded models/endpoints and the Step 1 exclusions. New signed capability, proposed `decide_local_release_v1`, must be distinct from `local_admission_allowed`; old Stage C grants cannot enable it. Require all needed dispatch, privacy, price, alias and fee facts in that signed authority. Preserve fail-closed behavior for missing/expired/unknown authority, insufficient local budget, unsupported options, revocation already observed or failed verification. Fresh lease acquisition/renewal remains online.

### What a cap can and cannot bound

For each boot-bound grant j with escrow L_j, allocate each attempt a conservative **hard upper bound B_i** before starting it, enforce `sum B_i ≤ L_j`, and never recycle decrements on refund, reserve refusal, process restart or lease transfer. If every delivered call's collectible charge is ≤ B_i, unreconciled delivered value is bounded by `sum_j L_j`, not “one request” and not necessarily one lease. A $1 grant permits up to $1 of such loss; four separately escrowed $1 grants permit up to $4. This is an exposure bound, not proof the customer will be billed: escrow remains a hold unless the settlement protocol has durable evidence and authority to debit it.

Existing authoritative minting reserves escrow transactionally, fences generations, binds grants to boot/key/region and limits open predecessors (currently three). Local ledger allocations are monotone and finalization never returns lease capacity. Those are foundations, not a proof for offline release. (`R:src/trusted_router/storage_gcp_spend_lease_authorize.py:63`, `R:src/trusted_router/storage_gcp_spend_lease_authorize.py:161`, `R:src/trusted_router/storage_gcp_spend_lease_authorize.py:192`, `R:src/trusted_router/storage_gcp_spend_lease_authorize.py:243`, `R:src/trusted_router/spend_lease_state.py:891`.)

Do not assume the generic Settings default of $1 is the live cap: trust-tier policy can select $5/$25/$100, additionally constrained by credit headroom, and issuance records the actual signed cap (`R:src/trusted_router/config.py:936`, `R:src/trusted_router/trust_eligibility.py:37`, `R:src/trusted_router/routes/internal/gateway.py:1677`, `R:scripts/deploy/rollout.sh:819`). Step 2 needs an explicit lower decide-specific cap and aggregate workspace outstanding-escrow limit, independent of larger chat tiers. Initial recommendation, if eventually approved: no more than $1 total offline decide authority for the pilot, inclusive of draining predecessors; no automatic growth from repeated restarts or rollover.

Today router receipt admission refuses late allocations after lease expiry plus skew; reconciliation can close authority that never reached the router (`R:src/trusted_router/spend_lease_state.py:957`). Step 2 must either deliver every allocation before that deadline or retain escrow and accept cryptographically proven in-window admissions through a specified recovery window. The former cannot guarantee availability during an outage. The latter needs new close/reconcile semantics: boot-bound monotonic admission sequence, durable replayable receipts/usage, finite recovery horizon, explicit handling of missing records and no release of “unused” escrow while offline obligations may exist. A RAM queue or restartable local disk file is not automatically durable or rollback-resistant in an enclave. No retroactive debit on a different authority and no cap billing for unknown usage by default.

### Non-streaming cap enforcement

Do not send Stage D `stream=true` heartbeats for decide. For hosted input-only billing, reserve the byte-plus-template ceiling using the highest eligible endpoint rate and request fee, prove vendor metering/cancellation and fallback expense bounds, and reject optimization when the bound does not fit. For native, reserve input including schema/provider template/cache-creation worst case plus the enforced output/reasoning maximum at every eligible endpoint's highest applicable rate. Carry these reservation bounds separately from actual usage and price-tier selection. Unsupported/mutable pricing or unknown template costs are synchronous misses.

Native's internal stream can support a local meter and provider cancellation before exhausting B_i, but byte/4 output estimates are not proof of exact vendor charges. Require provider hard-token-limit conformance and safety allowance for buffered/in-flight generation. Cancel at the threshold, discard an incomplete decision, never publish truncated probabilities; follow existing incomplete-generation refund semantics and count vendor loss. Hosted returns an atomic answer, so cancellation after receiving it cannot avoid already-incurred input expense. A timeout is not a dollar cap. Multi-host failures can incur multiple invoices even if only one customer generation settles; either reserve a separate operator fallback allowance or disable offline fallback until bounded. An over-bound result cannot be silently charged above the cap or released as if the proof held.

### Multiple enclaves, replay and revocation

A boot-bound token cannot be used by another boot under existing verification (`E:enclave-go/internal/spendlease/state.go:173`, `E:enclave-go/internal/spendlease/state.go:301`). But two boots for the same key can hold distinct grants or old/new generations. Offline old boots cannot observe a transfer/fence immediately. Each still-valid authority must have disjoint, retained escrow; subtract the grant's creating authorization and all online allocations as well as offline admissions before spending. Otherwise a fresh local counter can spend capacity the router has already allocated. Fence/predecessor limits and the current downward-only `ObserveReserve` are not replacements for this accounting proof.

Current local dedupe is only an in-flight process map; cross-enclave arbitration occurs at reserve, and router replay checks prevent a second dispatch only once their response is received (`E:enclave-go/internal/spendlease/state.go:184`, `E:enclave-go/internal/trustedrouter/spend_lease.go:206`). Step 2 can return two answers for the same idempotency key before that arbitration, even though settlement ultimately charges once. To retain existing idempotency semantics, require a proven single offline owner for a key/epoch with non-overlapping validity windows and local replay retention through the recovery horizon, or retain online arbitration for such requests. Single-region routing affinity alone is not ownership. Cross-boot cloning, counter reset and restored snapshots must not recreate spendable authority.

Key revocation, billing pause, price/route removal or a newly imposed key limit may be invisible until grant expiry. Treat the stale-authority window and maximum retained escrow as explicit accepted risks; do not imply the ordinary router's immediate checks still happen before every success. Stop issuing/renewing on pause or reconciliation staleness; cap cumulative loss across grants and time, not only per boot.

### Evidence before a Step 2 enablement proposal

Require a new money/security design review covering the withdrawn-addendum conflict, exact escrow and closure invariants, replay ownership, crash/rollback resistance and liability for unknown usage. Conformance/model tests must cover two boots spending one key, region transfer, last-unit races, expiry/clock skew, counter rollback, lost/duplicate/reordered reserve and settle, reaper/close races, vendor cap overrun, price/alias change and outage lasting beyond lease TTL. Kill the enclave at every reply-to-durability boundary and account for every microdollar as charged, held, refunded or explicitly lost. Run provider-specific bound tests and actual AnyEval compatibility tests. Establish p99 local overhead and externally measured loss/escrow observability. No flag in Step 1 authorizes this experiment.

## Rollout, observability and rollback

Supplied production state: router issuance/binding on, `TR_SPEND_LEASE_ADMISSION_ACCEPT=false`, pilot workspace as above; enclaves `QUILL_SPEND_LEASE_SHADOW=on`, no `SPEND_LEASE_LOCAL_ADMISSION`. Checked-in rollout agrees on router admission/pilot values (`R:scripts/deploy/rollout.sh:767`, `R:scripts/deploy/rollout.sh:823`). The enclave local flag is attested launch configuration, parsed as `on`, not an arbitrary runtime toggle (`E:enclave-go/internal/bootstrap/bootstrap_gcp.go:79`, `E:enclave-go/internal/bootstrap/bootstrap_gcp.go:117`).

| Controls | Use |
| --- | --- |
| Existing router `TR_SPEND_LEASE_ISSUANCE_ENABLED`, `TR_SPEND_LEASE_BINDING_ENABLED`, `TR_SPEND_LEASE_ADMISSION_ACCEPT`, pilot/trust/accepted-image/signing/ledger controls | Prerequisites; preserve trust, attestation and reconciliation gates. Acceptance requires binding, which requires issuance (`R:src/trusted_router/config.py:1577`). |
| Existing enclave `QUILL_SPEND_LEASE_SHADOW`, `SPEND_LEASE_LOCAL_ADMISSION` | Keep shadow; local flag enables Stage C machinery only. Turning it on also reaches existing chat code, so require a route allowlist before a decide-only canary. |
| **Proposed**, default-off router `TR_DECIDE_SPEND_LEASE_ENABLED`; explicit decide model/workspace allowlists | Gate decide issuance and new receipt acceptance without widening Stage D or the global pilot. |
| **Proposed**, attested/default-off enclave `QUILL_DECIDE_SPEND_LEASE_ADMISSION`; route allowlist defaulting to no new routes | Native/hosted opt-in with independent alias capability gate. Measure/approve image and launch flags before router trusts it. |
| **Proposed**, default-off enclave `QUILL_DECIDE_ASYNC_SETTLE`, ticket/count/amount limits; router-authorized cohort response bit | Step 1b only after loss-policy/client gates. Require accepted decide lease and handoff capacity. |
| Existing `QUILL_SETTLEMENT_RETRY_*`, `TR_SETTLE_OUTBOX_ENABLED` and drain worker | Repair infrastructure; preserve current jobs. Decide non-evicting handoff must be independently bounded. |
| Existing Stage D `QUILL_USAGE_HEARTBEAT`, `QUILL_TERMINATE_AT_CAP`, `TR_STAGE_D_HEARTBEAT_ENABLED`, `TR_STAGE_D_ELIGIBILITY_ENABLED`, `TR_REAP_SNAPSHOT_BOOKING_ENABLED` | Leave unchanged; they do not enable decide (`E:enclave-go/cmd/enclave/stage_d.go:32`, `R:src/trusted_router/routes/internal/gateway.py:3103`). |
| **Future proposed**, two-sided `*_DECIDE_LOCAL_RELEASE` and signed capability | Step 2 only after a separate approval/review; remain absent/off throughout Step 1. |

Enable in this order:

1. Land shared protocol fixtures, money/candidate/alias tests and observability with all new gates off. Test all six public paths, both provider types, both native attempts, explicit attribution and batch exclusions, and old/new router-enclave pairings. Run differential/conformance tests for reserve/settle/refund and existing Stage C/D suites. This document itself needs only citation/diff checks, not production-code tests.
2. Deploy router support in issuance/shadow mode for decide; keep local admission and async off. Compare candidate order, policy hash, estimates, actual costs and exclusion reasons against the synchronous path. Acceptance-dependent dispatch fields must be exercised in fixtures; today's `freeze_spend_lease_catalog(stage_c=False)` strips dispatch fields (`R:src/trusted_router/spend_leases.py:420`).
3. Deploy approved enclave images with code present but local flags off. Register/accept boot digests and signing manifest; verify artifact/launch policy. Configure a decide-only route gate so flipping `SPEND_LEASE_LOCAL_ADMISSION` does not also launch an unreviewed chat pilot.
4. Enable router Stage C acceptance plus the narrow decide gate first; retain endpoint, workspace/model and image allowlists. Let old grants expire/refresh into the new cohort. Then enable local admission on one enclave in us-central1, **blocking settlement**. Check cancellation/unmarked/version-skew paths under fault injection before increasing traffic. This acceptance flag is global today; the new route gates are required isolation, not optional documentation.
5. Only after Joseph's loss approval and AnyEval gate, enable Step 1b for Jev first with the proposed ticket/amount limits; then ordinary native IDs, then named aliases after alias parity tests. Canary other regions after A1 measurements; do not conflate A1 wins with admission wins. Expand beyond Joseph's workspace only in a later reviewed cohort change.

Record lease-hit/miss reason, policy/catalog mismatch, signature/boot failures, reserve marked/unmarked/refused/timeout, selected candidate/fallback and native attempt counts, discarded-speculation counts/estimated vendor expense, response-before-durable-intent count/age/value, ticket utilization, retry drops/give-ups, outbox lag/dead/quarantine, refunded reaps, `settle_lost`, actual-minus-estimate/clamp amounts, lease utilization/escrow and client errors/Retry-After. Use IDs/hashes and bounded classification labels, never prompts/questions/answers or bearer/lease tokens. Alert on any cohort/metadata mismatch, double settlement or exposure-limit breach; pause async on unresolved billing beyond the chosen recovery SLO (initial pilot: five minutes), any handoff eviction, or daily loss-budget exhaustion. Queue saturation alone falls back synchronously before reply. Tail latency improvement never overrides a money invariant.

Rollback new async admissions first, then new speculative admissions, with existing jobs/accepted authorizations drained and repaired under their original authority. Keep router settlement/outbox/reconciliation and old token/image verification support until all issued grants expire and outstanding work reaches terminal state. Disable decide issuance/acceptance only after enclave routing is back to synchronous when possible; emergency router rejection is safe but causes cancelled paid speculation and errors until enclave rollback. Do not erase queues, reclaim outstanding escrow early, rewrite settled refunds, or disable global binding beneath live decide leases. If Step 2 were ever enabled, router flag-off alone could not stop unreachable enclaves before their signed offline authority expires; its rollback plan must budget that window explicitly.

## Review-sized PR sequence

No PRs are created by this design task. Each implementation PR must preserve flag-off behavior and carry its own failure tests.

| PR | Repository | Bounded change and evidence |
| --- | --- | --- |
| R1 | quill-router | Decide cohort/schema and gated issuance/acceptance for hosted and ordinary native IDs; canonical estimate/provider fixtures, all-candidate Credits and exclusion tests. Stage D unchanged. |
| E1 | quill-cloud-proxy | Shared decide admission descriptor, hosted builder, new-cohort verification and normalization fixtures. Default-off route gate and attested flag wiring; no handler reordering. |
| R2 / E2 | router / enclave, paired | Signed private-alias mapping/chain envelope, requested versus resolved IDs, metadata hiding and acceptance matching; alias-change/version-skew tests. Keep aliases synchronous until both deployed. |
| E3 | quill-cloud-proxy | Native helper split and per-attempt speculative collection/reserve gate, blocking settlement. Provider fallback, cancellation, generation-recorder, idempotency and two-attempt tests. |
| E4 | quill-cloud-proxy | Hosted speculation using decide descriptor, blocking settlement. Vendor fallback, refusal, validation/refund, usage-clamp and unmarked-redispatch tests. |
| E5 | quill-cloud-proxy | Non-evicting decide handoff/worker, hosted retry parity, exact settlement job identity and disposition handling, drain/capacity/death tests; async still off. |
| R3 / E6 | router / enclave | Narrow async cohort authorization and guarded reply-before-settle, batch/receipt/attribution/broadcast exclusions, monetary/response parity and real client tests. Separate enabling config PR records Joseph's risk decision. |
| Ops follow-up | both | Config-as-code canary flips, per-region A1 comparison, dashboards and rollback drill, without broadening cohort. |

Step 2 is a separate design and later series: router escrow/recovery/replay protocol, enclave durable admission ownership and cap enforcement, then two-sided capability and canary. Do not append offline release to an E3/E4 “latency cleanup” PR.

## Decisions for Joseph

1. **Extend lease billing policy to decide, including decision-50 capped charges?** Recommend the narrow paid Credits pilot, retaining actual metering and absorbing any amount above allocation. Approve the explicit cohort exception; do not promise exact equivalence to ordinary billing or increase customer charges to cover estimates.
2. **Accept valid replies before durable billing evidence, with possible free answers and delayed billing visibility?** Recommend an opt-in Jev-first pilot only after the client/repair gates, with $0.01 per attempt, 100 outstanding attempts per enclave and a $1 pilot-wide daily lost/unresolved-charge stop threshold as initial controls; the latter is not a hard global ceiling during detection lag. This includes continuing native attempt 2 while attempt 1 settles asynchronously and returning 200 where a settle failure previously produced spent 502. If that risk or contract change is unacceptable, keep settlement blocking; do not charge unproven usage on reaping.
3. **Relax the fail-closed first-byte rule to pursue ~1 ms for fast models?** Recommend **not yet**. Step 1 cannot guarantee that target when reserve exceeds model time. If the latency is worth offline delivery, authorize a new decide-only risk policy, including stale revocation, duplicate-execution/ownership constraints, retained escrow and unrecoverable usage, with at most $1 total offline pilot authority initially. Require the Step 2 evidence and independent money/security review before any enabling flag.
