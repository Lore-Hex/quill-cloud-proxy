# Task: enclave audit batch B — translation correctness (temperature, finish_reason, thinking, metadata)

Enclave (`enclave-go`). Branch off origin/main AFTER audit batch A merges
(shares adapter.go / types.go). Fixes the quill-router#130 translation-
correctness findings. BUILD + TEST ONLY — no commit/push/deploy. Do not touch
`.codex-*` files. Ground each change in the cited file.

## 1. finish_reason: stop hiding truncation & safety (HIGH — the reported theme)

- `internal/llm/vertex_gemini.go` `mapGeminiFinishReason` (~:720): the default
  case returns `end_turn` for SAFETY/RECITATION/BLOCKLIST/PROHIBITED_CONTENT/
  SPII/OTHER, and adapter.go `mapStopReason` (~:876) turns end_turn → OpenAI
  `stop`. Map SAFETY/BLOCKLIST/PROHIBITED_CONTENT/SPII → a distinct reason
  that carries to OpenAI `content_filter`; RECITATION and MAX_TOKENS → `length`.
  Anthropic has no content_filter, so add a synthetic stop_reason marker that
  `mapStopReason` recognizes and emits as OpenAI `content_filter`/`length`
  rather than collapsing to `stop`.
- OpenAI-compatible content_filter: an upstream finish_reason=content_filter
  routed through stream_translate → Anthropic end_turn → OpenAI stop loses the
  filter signal. Carry the marker through so it restores to content_filter in
  mapStopReason.

## 2. temperature range / Vertex opus guard (MEDIUM)

- Anthropic temperature max is 1.0; OpenAI is 2.0. `anthropicTemperature`
  (byok.go ~:373) omits for opus-4.7/4.8 but never clamps range. Clamp
  temperature to <= 1.0 for Anthropic-family targets (or scale 0-2 → 0-1;
  pick clamp for predictability — a value >1 becomes 1.0). Apply in the shared
  anthropicTemperature helper so all Anthropic paths get it.
- `gcp.go` (Vertex) sends `Temperature: body.Temperature` raw and does NOT
  call anthropicTemperature — so opus-4.7/4.8 400 on Vertex. Wrap it:
  `anthropicTemperature(directModelID(...), body.Temperature)`.

## 3. Anthropic param drops on Vertex / OpenRouter upstreams (HIGH/MEDIUM)

- `gcp.go` reqBody: add `StopSequences []string json:"stop_sequences,omitempty"`
  (= body.StopSequences), `Thinking any json:"thinking,omitempty"`
  (= body.Thinking), and `Metadata any json:"metadata,omitempty"`. Vertex
  currently drops all three.
- `openrouter.go` invokeOne Anthropic path: forward body.Thinking (OpenRouter
  accepts native Anthropic params on Claude models); add the field.
- `metadata.user_id`: add `Metadata map[string]any json:"metadata,omitempty"`
  to AnthropicMessagesRequest (types.go), copy it in MessagesToAnthropic
  (messages.go), and forward on the anthropic-direct/BYOK/Vertex outbound
  bodies.

## 4. max_tokens 4096 re-introduction on OpenRouter/Vertex Anthropic (MEDIUM)

`openrouter.go invokeOne` and `gcp.go` send `body.MaxTokens` unconditionally,
so even when the client set no cap they forward the 4096 DefaultMaxTokens.
Mirror the byok.go guard: only set reqBody.MaxTokens when
`body.MaxTokensExplicit` is true (for the OpenAI-compatible passthrough).
NOTE: Anthropic REQUIRES max_tokens, so for the Anthropic-native bodies keep
sending the value (Anthropic can't omit it) — apply the explicit-guard only
where the upstream allows omission (the OpenAI-compatible openrouter path).
For the Anthropic-native Vertex/OR path where max_tokens is required, leave
the default but do NOT lower it further. (Be precise: don't break Anthropic's
required-field contract.)

## Tests (`internal/adapter`, `internal/llm`)
- mapGeminiFinishReason: SAFETY→content_filter-marker→OpenAI content_filter;
  RECITATION/MAX_TOKENS→length; STOP→stop.
- anthropicTemperature clamps >1.0 to 1.0 for Anthropic; Vertex path applies it.
- gcp.go/openrouter bodies include stop_sequences/thinking/metadata when set.
- MessagesToAnthropic copies metadata.user_id through.

## Gates
`cd enclave-go && GOCACHE=$(mktemp -d) go build ./internal/... ./cmd/... 2>&1 | grep -v 'build constraints exclude'`; `go vet ./internal/adapter ./internal/llm ./internal/types`; `GOCACHE=$(mktemp -d) go test ./internal/adapter/ ./internal/types/ ./internal/llm/` (report TEE-tag / localhost-bind exclusions as expected).

## Constraints
- Absent params change nothing. Do NOT break Anthropic's required max_tokens.
- BUILD + TEST ONLY.
